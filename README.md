# MMS-1B-ALL Fine-tuning with Adapters

Fine-tuning of the **Facebook MMS-1B-all** model using **parameter-efficient adapters** for ASR tasks, trained on a portion of the **ASR Fellowship Challenge dataset**.
